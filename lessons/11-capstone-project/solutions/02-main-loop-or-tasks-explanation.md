# Explanation 02: Main Loop or Tasks

This flow is intentionally simple. A single clear polling loop is usually easier
to understand than premature concurrency.

You can always split it into multiple tasks later once the logic is stable.
