# Exercise 01: Add Display Message

Add a new message variant for updating the display text.

Then update `encode_message`.

Requirements:

- include the text in the encoded message
- choose a simple text format that is readable by humans
- make sure the variant name and encoding stay consistent

What this trains:

- extending a protocol safely
- keeping message design coherent
