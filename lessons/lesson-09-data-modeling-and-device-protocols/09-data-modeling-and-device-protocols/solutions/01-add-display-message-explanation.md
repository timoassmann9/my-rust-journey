# Explanation 01: Add Display Message

The design stays coherent because the new variant follows the same basic rule as
the older ones: one clear enum variant maps to one clear encoded representation.

That consistency is more important than choosing the perfect protocol format on
day one.
